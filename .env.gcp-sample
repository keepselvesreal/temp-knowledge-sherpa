# GCP VM 배포 시 사용할 운영 환경 샘플
# (실제 배포 전에 비밀번호를 강력한 임의값으로 변경)

# ============================================
# 📦 Database Configuration (운영)
# ============================================

# 운영 데이터베이스명
DB_NAME=wordpress_prod

# 운영 MySQL 사용자명
DB_USER=wp_user

# 운영 MySQL 사용자 비밀번호 (샘플, 실제 배포 시 변경 필수)
DB_PASSWORD=Prod@Secure_Pass_2026_QwErTy

# 운영 MySQL Root 비밀번호 (샘플, 실제 배포 시 변경 필수)
DB_ROOT_PASSWORD=Root@Prod_2026_AsdfGhJk

# ============================================
# 🔧 WordPress Debug Mode (운영)
# ============================================

# 운영 환경: false (에러 출력 비활성화)
DEBUG_MODE=false

# ============================================
# 🐳 Docker User Permission (운영)
# ============================================

# GCP VM 기본 사용자 UID
UID=1000

# GCP VM 기본 사용자 GID
GID=1000
