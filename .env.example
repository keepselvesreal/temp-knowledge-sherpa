# ============================================
# WordPress Playground 환경 설정
# ============================================
#
# 로컬 개발: 기본값 그대로 사용
# GCP VM 운영: 운영 섹션의 샘플을 참고하여 수정
#

# ============================================
# 📦 Database Configuration
# ============================================

# 데이터베이스명
# 로컬: wordpress_playground
# 운영: wordpress_prod
DB_NAME=wordpress_playground

# MySQL 사용자명
# 로컬: wp_playground
# 운영: wp_user 또는 custom (권장: 운영전용 사용자)
DB_USER=wp_playground

# MySQL 사용자 비밀번호
# 보안: 최소 16자, 특수문자 포함
# 로컬: playground_secure_pass_123 (개발용)
# 운영: 강력한 임의 비밀번호 생성 필수
DB_PASSWORD=playground_secure_pass_123

# MySQL Root 비밀번호
# 보안: 로컬과 운영이 다른 비밀번호 필수
# 로컬: root_secure_pass_123 (개발용)
# 운영: 강력한 임의 비밀번호 생성 필수
DB_ROOT_PASSWORD=root_secure_pass_123

# ============================================
# 🔧 WordPress Debug Mode
# ============================================

# WordPress 디버그 모드
# true: 로컬 개발 (에러 출력 활성화)
# false: 운영 환경 (에러 숨김)
DEBUG_MODE=true

# ============================================
# 🐳 Docker User Permission
# ============================================

# 호스트 사용자 UID (자동 감지: id -u)
# 로컬: $(id -u) 명령어로 자동 설정
# 운영: 1000 (기본값)
UID=1000

# 호스트 사용자 GID (자동 감지: id -g)
# 로컬: $(id -g) 명령어로 자동 설정
# 운영: 1000 (기본값)
GID=1000

# ============================================
# 📝 운영 환경 샘플 (.env.prod)
# ============================================
#
# GCP VM 배포 시 다음 값으로 수정:
#
# DB_NAME=wordpress_prod
# DB_USER=wp_user
# DB_PASSWORD=xxxxxxxxxxxxxxxxxxxxx (강력한 암호)
# DB_ROOT_PASSWORD=xxxxxxxxxxxxxxxxxxxxx (강력한 암호)
# DEBUG_MODE=false
# UID=1000
# GID=1000
